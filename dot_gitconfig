[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true    # use n and N to move between diff sections

    # delta detects terminal colors automatically; set one of these to disable auto-detection
    # dark = true
    # light = true

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default

[user]
        name = Gianguido Sorà
        email = gsora@users.noreply.github.com
	signingkey = ~/.ssh/id_ed25519.pub

[push]
	autoSetupRemote = true
[gpg]
	format = ssh
[commit]
	verbose = true
	sshsign = true
	gpgsign = true
[init]
	defaultBranch = main
